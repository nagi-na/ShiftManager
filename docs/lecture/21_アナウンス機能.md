# 21章 アナウンス機能（投稿・添付・未読・自動削除）

> 発展編（付録）。**9章まで＋[7章（認証付き配信）](07_確定シフトのアップロード.md)** が終わっている前提です。
> 機能追加の「簡潔版」です。Python は全文、テンプレートは要点を載せ、長いHTMLはリポジトリ（`shifts/templates/shifts/…`）と [`../features/announcements.md`](../features/announcements.md) を参照します。

---

## 21-0. この章で作るもの

確定シフトの公開や新しい期間の追加を、クルーに**まとめて周知**する仕組みです。

- リーダー/管理者が**手動投稿**（タイトル・レベル・本文・画像/PDF添付）。
- よくある通知は**自動投稿**（期間追加時。確定シフトは「アナウンスを投稿」ボタン）。
- クルーには**未読バッジ**。一覧→**詳細を開いた時点で既読**（クルーごと）。
- アナウンスは**投稿から1週間で自動削除**（cron不要・表示時に掃除）。
- 添付は**ログイン必須の認証付き配信**（7章 `confirmed_shift_file` と同じ X-Accel-Redirect 方式）。

| レベル | 値 | マーク | 色 |
| --- | --- | --- | --- |
| 情報 | info | ℹ️ | 青 |
| 注意 | warning | ⚠️ | 黄 |
| 警告 | danger | ⛔ | 赤 |
| 完了 | success | ✅ | 緑 |

> 値は Bootstrap の `alert-*` / `bg-*` に揃えてあるので、テンプレートで `alert-{{ a.level }}` のように使えます。

---

## 21-1. モデル（`shifts/models.py`）

4つのモデルを足します。

```python
class AnnouncementSettings(models.Model):
    """自動投稿のオンオフ（シングルトン：常に1行）。"""

    auto_on_period = models.BooleanField("対象期間の追加時に自動投稿", default=True)

    class Meta:
        verbose_name = "アナウンス自動投稿設定"
        verbose_name_plural = "アナウンス自動投稿設定"

    def __str__(self):
        return "アナウンス自動投稿設定"

    def save(self, *args, **kwargs):
        self.pk = 1  # 常に1行に固定
        super().save(*args, **kwargs)

    @classmethod
    def load(cls):
        obj, _ = cls.objects.get_or_create(pk=1)
        return obj


class Announcement(models.Model):
    """クルーへのアナウンス。手動投稿と自動投稿（確定シフト/期間追加）。"""

    class Category(models.TextChoices):
        MANUAL = "manual", "アナウンス"
        CONFIRMED = "confirmed", "確定シフト"
        PERIOD = "period", "期間追加"

    class Level(models.TextChoices):
        INFO = "info", "情報"
        WARNING = "warning", "注意"
        DANGER = "danger", "警告"
        SUCCESS = "success", "完了"

    title = models.CharField("タイトル", max_length=100)
    body = models.TextField("本文", blank=True)
    category = models.CharField("種別", max_length=16, choices=Category.choices, default=Category.MANUAL)
    level = models.CharField("表示レベル", max_length=16, choices=Level.choices, default=Level.INFO)
    related_period = models.ForeignKey(
        ShiftPeriod, verbose_name="関連する対象期間",
        on_delete=models.SET_NULL, null=True, blank=True, related_name="announcements",
    )
    created_by = models.ForeignKey(
        settings.AUTH_USER_MODEL, verbose_name="投稿者",
        on_delete=models.SET_NULL, null=True, blank=True, related_name="announcements",
    )
    created_at = models.DateTimeField("投稿日時", auto_now_add=True)

    class Meta:
        verbose_name = "アナウンス"
        verbose_name_plural = "アナウンス"
        ordering = ["-created_at"]

    def __str__(self):
        return self.title

    @property
    def level_icon(self):
        return {
            self.Level.INFO: "ℹ️", self.Level.WARNING: "⚠️",
            self.Level.DANGER: "⛔", self.Level.SUCCESS: "✅",
        }.get(self.level, "ℹ️")


def announcement_upload_path(instance, filename):
    return f"announcements/{instance.announcement_id}/{filename}"


class AnnouncementAttachment(models.Model):
    """アナウンスの添付ファイル（画像・PDF等）。"""

    announcement = models.ForeignKey(
        Announcement, verbose_name="アナウンス",
        on_delete=models.CASCADE, related_name="attachments",
    )
    file = models.FileField("ファイル", upload_to=announcement_upload_path)
    original_name = models.CharField("元ファイル名", max_length=255, blank=True)

    def __str__(self):
        return self.original_name or self.file.name

    @property
    def is_image(self):
        name = (self.original_name or self.file.name).lower()
        return name.endswith((".png", ".jpg", ".jpeg", ".gif", ".webp"))


class AnnouncementRead(models.Model):
    """アナウンスの既読記録（ユーザー×アナウンスで1件）。"""

    announcement = models.ForeignKey(
        Announcement, verbose_name="アナウンス",
        on_delete=models.CASCADE, related_name="reads",
    )
    user = models.ForeignKey(
        settings.AUTH_USER_MODEL, verbose_name="閲覧者",
        on_delete=models.CASCADE, related_name="announcement_reads",
    )
    read_at = models.DateTimeField("既読日時", auto_now_add=True)

    class Meta:
        constraints = [
            models.UniqueConstraint(fields=["announcement", "user"], name="unique_announcement_read"),
        ]
```

```
$ python manage.py makemigrations
$ python manage.py migrate
```

> 🔰 **シングルトン**（`AnnouncementSettings`）: `save` で必ず `pk=1` にし、`load()` で取得/生成するので、設定行は常に1つだけです。「未読」は `AnnouncementRead` の**行が無い**状態、「既読」は**行がある**状態で表します。

---

## 21-2. フォーム（`shifts/forms.py`）

添付はフォームではなくビューで `request.FILES` から扱うので、フォームはテキスト項目だけです。

```python
class AnnouncementForm(forms.ModelForm):
    class Meta:
        model = Announcement
        fields = ["title", "level", "body"]
        widgets = {
            "title": forms.TextInput(attrs={"class": "form-control"}),
            "level": forms.Select(attrs={"class": "form-select"}),
            "body": forms.Textarea(attrs={"class": "form-control", "rows": 4}),
        }


class AnnouncementSettingsForm(forms.ModelForm):
    class Meta:
        model = AnnouncementSettings
        fields = ["auto_on_period"]
        widgets = {"auto_on_period": forms.CheckboxInput(attrs={"class": "form-check-input"})}
```

---

## 21-3. ビュー（`shifts/views.py`）

import に4モデルとフォーム2つ、`mimetypes` / `quote`（添付配信用、7章で導入済み）を加えます。

### 掃除・未読・自動投稿のヘルパー

```python
ANNOUNCEMENT_RETENTION_DAYS = 7  # 投稿からこの日数を過ぎたら自動削除


def _prune_old_announcements():
    """投稿から一定日数を過ぎたアナウンスを、添付の実ファイルごと削除する。

    cron 等を使わず、アナウンスを表示するタイミングで掃除する方式。
    """
    cutoff = timezone.now() - timedelta(days=ANNOUNCEMENT_RETENTION_DAYS)
    old = Announcement.objects.filter(created_at__lt=cutoff)
    for ann in old.prefetch_related("attachments"):
        for att in ann.attachments.all():
            att.file.delete(save=False)
    old.delete()


def _unread_announcement_count(user):
    """このユーザーの未読アナウンス件数。"""
    return Announcement.objects.exclude(reads__user=user).count()


def _auto_announce(category, title, body="", period=None, by=None, level=Announcement.Level.INFO):
    """設定がオンのときだけ、自動アナウンスを投稿する。"""
    cfg = AnnouncementSettings.load()
    enabled = {Announcement.Category.PERIOD: cfg.auto_on_period}.get(category, False)
    if not enabled:
        return None
    return Announcement.objects.create(
        title=title, body=body, category=category, level=level,
        related_period=period, created_by=by,
    )
```

> 🔰 **未読数 `exclude(reads__user=user)`**: 「自分の既読行がぶら下がっていないアナウンス」を数えます。これ1本で未読件数が出ます。

### クルー側：一覧・詳細・添付配信

```python
@login_required
def announcements(request):
    """アナウンス一覧（タイトル＋抜粋）。既読は詳細を開いたときに付ける。"""
    _prune_old_announcements()
    items = list(
        Announcement.objects.select_related("created_by").prefetch_related("attachments").all()
    )
    read_ids = set(
        AnnouncementRead.objects.filter(user=request.user, announcement__in=items)
        .values_list("announcement_id", flat=True)
    )
    for a in items:
        a.is_unread = a.id not in read_ids
    return render(request, "shifts/announcements.html", {"items": items})


@login_required
def announcement_detail(request, pk):
    """アナウンス詳細。開いた時点でこのユーザーの既読にする。"""
    _prune_old_announcements()
    ann = get_object_or_404(
        Announcement.objects.select_related("created_by").prefetch_related("attachments"), pk=pk
    )
    AnnouncementRead.objects.get_or_create(announcement=ann, user=request.user)
    return render(request, "shifts/announcement_detail.html", {"a": ann})


@login_required
def announcement_attachment(request, pk):
    """添付の配信。ログイン必須＋本番は X-Accel-Redirect（7章 confirmed_shift_file と同方式）。"""
    att = get_object_or_404(AnnouncementAttachment, pk=pk)
    name = att.file.name
    content_type = mimetypes.guess_type(name)[0] or "application/octet-stream"
    display_name = att.original_name or name.rsplit("/", 1)[-1]
    disposition = "inline; filename*=UTF-8''" + quote(display_name)

    if settings.DEBUG:
        response = FileResponse(att.file.open("rb"), content_type=content_type)
    else:
        response = HttpResponse(content_type=content_type)
        response["X-Accel-Redirect"] = "/protected/" + quote(name)
    response["Content-Disposition"] = disposition
    response["Cache-Control"] = "private, no-store"
    return response
```

> 🔰 添付配信は7章で作った `/protected/`（internal・mediaルート）の仕組みを**そのまま再利用**します。Nginx 設定の追加は不要です。

### 管理側：投稿・編集・削除・設定

添付の検証（拡張子・サイズ）を共通関数にします。

```python
ANNOUNCE_ALLOWED_EXT = (".png", ".jpg", ".jpeg", ".gif", ".webp", ".pdf")
ANNOUNCE_MAX_SIZE = 10 * 1024 * 1024  # 10MB


def _announcement_file_errors(files):
    """添付（画像/PDF・各10MB）の検証エラー文言リストを返す。"""
    errors = []
    for f in files:
        if not f.name.lower().endswith(ANNOUNCE_ALLOWED_EXT):
            errors.append(f"{f.name}: 画像かPDFを選んでください。")
        elif f.size > ANNOUNCE_MAX_SIZE:
            errors.append(f"{f.name}: 10MBまでにしてください。")
    return errors


@manager_required
def manage_announcements(request):
    """手動投稿（画像・PDFを複数添付可）と一覧・削除。"""
    _prune_old_announcements()
    if request.method == "POST":
        form = AnnouncementForm(request.POST)
        files = request.FILES.getlist("files")
        file_errors = _announcement_file_errors(files)
        if form.is_valid() and not file_errors:
            ann = form.save(commit=False)
            ann.created_by = request.user
            ann.category = Announcement.Category.MANUAL
            ann.save()
            for f in files:
                AnnouncementAttachment.objects.create(announcement=ann, file=f, original_name=f.name)
            messages.success(request, "アナウンスを投稿しました。")
            return redirect("manage_announcements")
        for e in file_errors:
            messages.error(request, e)
    else:
        form = AnnouncementForm()

    items = Announcement.objects.prefetch_related("attachments").all()
    return render(request, "shifts/manage_announcements.html", {"form": form, "items": items})


@manager_required
def manage_announcement_edit(request, pk):
    """編集（タイトル・レベル・本文の変更、添付の追加・削除）。"""
    ann = get_object_or_404(Announcement.objects.prefetch_related("attachments"), pk=pk)
    if request.method == "POST":
        form = AnnouncementForm(request.POST, instance=ann)
        files = request.FILES.getlist("files")
        file_errors = _announcement_file_errors(files)
        remove_ids = request.POST.getlist("remove_attachments")
        if form.is_valid() and not file_errors:
            form.save()
            for att in ann.attachments.filter(pk__in=remove_ids):
                att.file.delete(save=False)
                att.delete()
            for f in files:
                AnnouncementAttachment.objects.create(announcement=ann, file=f, original_name=f.name)
            messages.success(request, "アナウンスを更新しました。")
            return redirect("manage_announcements")
        for e in file_errors:
            messages.error(request, e)
    else:
        form = AnnouncementForm(instance=ann)
    return render(request, "shifts/manage_announcement_edit.html", {"form": form, "ann": ann})


@manager_required
def manage_announcement_delete(request, pk):
    """削除（添付の実ファイルも削除）。"""
    ann = get_object_or_404(Announcement, pk=pk)
    if request.method == "POST":
        for att in ann.attachments.all():
            att.file.delete(save=False)
        ann.delete()
        messages.success(request, "アナウンスを削除しました。")
    return redirect("manage_announcements")


@manager_required
def manage_announcement_settings(request):
    """自動投稿のオンオフ設定。"""
    obj = AnnouncementSettings.load()
    if request.method == "POST":
        form = AnnouncementSettingsForm(request.POST, instance=obj)
        if form.is_valid():
            form.save()
            messages.success(request, "自動投稿の設定を保存しました。")
            return redirect("manage_announcement_settings")
    else:
        form = AnnouncementSettingsForm(instance=obj)
    return render(request, "shifts/manage_announcement_settings.html", {"form": form})
```

### 既存ビューへの差し込み

**ホーム（4章 `home`）** に未読数を渡します。

```python
    unread = _unread_announcement_count(request.user)
    # 各 render の context に "unread_announcements": unread を追加
```

**期間追加（8/9章 `manage_periods`）** で、表示ONの期間を作ったとき自動投稿します。

```python
            if period.is_visible:
                deadline = timezone.localtime(period.deadline).strftime("%Y/%m/%d %H:%M")
                _auto_announce(
                    Announcement.Category.PERIOD,
                    title=f"新しいシフト期間が追加されました（{period}）",
                    body=f"締切は {deadline} です。提出をお願いします。",
                    period=period, by=request.user,
                )
```

**確定シフト（7章 `confirmed_shift`）** の POST に「アナウンスを投稿」ボタンの分岐を足します（自動ではなくボタンで明示投稿）。

```python
        if request.POST.get("announce"):
            Announcement.objects.create(
                title=f"確定シフトが公開されました（{period}）",
                body="「確定シフト」のページからご確認ください。",
                category=Announcement.Category.CONFIRMED,
                level=Announcement.Level.SUCCESS,
                related_period=period, created_by=request.user,
            )
            messages.success(request, "確定シフトのアナウンスをクルーに投稿しました。")
            return redirect("confirmed_shift", pk=pk)
```

---

## 21-4. URL（`shifts/urls.py`）

```python
    path("announcements/", views.announcements, name="announcements"),
    path("announcements/<int:pk>/", views.announcement_detail, name="announcement_detail"),
    path("announcement-files/<int:pk>/", views.announcement_attachment, name="announcement_attachment"),
    path("manage/announcements/", views.manage_announcements, name="manage_announcements"),
    path("manage/announcements/<int:pk>/edit/", views.manage_announcement_edit, name="manage_announcement_edit"),
    path("manage/announcements/<int:pk>/delete/", views.manage_announcement_delete, name="manage_announcement_delete"),
    path("manage/announcement-settings/", views.manage_announcement_settings, name="manage_announcement_settings"),
```

---

## 21-5. テンプレート（要点）

### 一覧 `announcements.html`

各行はレベル色（`list-group-item-{{ a.level }}`）＋マーク＋未読バッジ。タップで詳細へ。

```html
<div class="list-group">
  {% for a in items %}
  <a href="{% url 'announcement_detail' a.pk %}"
     class="list-group-item list-group-item-action list-group-item-{{ a.level }}">
    <div class="fw-semibold">
      <span class="me-1">{{ a.level_icon }}</span>
      {% if a.is_unread %}<span class="badge bg-danger me-1">未読</span>{% endif %}
      {{ a.title }}
    </div>
    {% if a.body %}<div class="small text-muted">{{ a.body|truncatechars:60 }}</div>{% endif %}
    <div class="small text-muted">
      {{ a.created_at|date:"Y/m/d H:i" }}・{{ a.get_category_display }}
      {% if a.attachments.count %}・📎{{ a.attachments.count }}{% endif %}
    </div>
  </a>
  {% endfor %}
</div>
```

### 詳細 `announcement_detail.html`（画像はインライン・他はリンク）

```html
<div class="alert alert-{{ a.level }}">
  <h2 class="h5 mb-1"><span class="me-1">{{ a.level_icon }}</span>{{ a.title }}</h2>
  <p class="small mb-0 opacity-75">{{ a.created_at|date:"Y/m/d H:i" }}{% if a.created_by %} ・ {{ a.created_by.name }}{% endif %}</p>
</div>
{% if a.body %}<p style="white-space: pre-wrap;">{{ a.body }}</p>{% endif %}
{% for att in a.attachments.all %}
  {% if att.is_image %}
    <a href="{% url 'announcement_attachment' att.pk %}" target="_blank">
      <img src="{% url 'announcement_attachment' att.pk %}" class="img-fluid rounded border mb-2" style="max-height:480px;">
    </a>
  {% else %}
    <div class="mb-1">
      <a class="btn btn-outline-secondary btn-sm" href="{% url 'announcement_attachment' att.pk %}" target="_blank">📎 {{ att.original_name|default:"添付ファイル" }}</a>
    </div>
  {% endif %}
{% endfor %}
```

> 🔰 本文は `white-space: pre-wrap;` で改行を保ったまま表示します。Django の自動エスケープが効くので、ユーザーが書いた `<` などはそのまま文字として安全に表示されます（`|safe` は使いません）。

### 管理 `manage_announcements.html`（投稿フォーム＋一覧）

投稿フォームは `enctype="multipart/form-data"` が必須。添付は素の `<input type="file" name="files" multiple>`（ビューが `request.FILES.getlist("files")` で受けます）。一覧は各行に編集リンクと削除フォーム。

```html
<form method="post" enctype="multipart/form-data">
  {% csrf_token %}
  {{ form.title }} {{ form.level }} {{ form.body }}
  <input type="file" name="files" multiple accept="image/*,application/pdf" class="form-control">
  <button type="submit" class="btn btn-primary btn-sm">投稿する</button>
</form>
```

### その他

- **`manage_announcement_settings.html`**: `{{ form.auto_on_period }}` のチェック1つ。確定シフトはボタン投稿なので設定なし、と案内。
- **`manage_announcement_edit.html`**: 投稿フォームと似た形。既存添付を一覧し、各添付に `name="remove_attachments" value="{{ att.pk }}"` のチェックを置くと削除できる（ビューの `getlist("remove_attachments")` が受ける）。
- **ホーム `home.html`**: 「アナウンス」ボタンに未読バッジ。

  ```html
  <a class="btn btn-outline-secondary btn-sm position-relative" href="{% url 'announcements' %}">
    アナウンス
    {% if unread_announcements %}
      <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">{{ unread_announcements }}</span>
    {% endif %}
  </a>
  ```

- **確定シフト `confirmed.html`**: 「アナウンスを投稿」ボタン（`<input type="hidden" name="announce" value="1">`）。
- **管理メニュー `manage_top.html`**: 「アナウンス管理」カード。

実物のHTMLはリポジトリ（`shifts/templates/shifts/…`）を写経してください。

---

## 21-6. 管理画面に登録（任意・`shifts/admin.py`）

```python
class AnnouncementAttachmentInline(admin.TabularInline):
    model = AnnouncementAttachment
    extra = 0


@admin.register(Announcement)
class AnnouncementAdmin(admin.ModelAdmin):
    list_display = ("title", "category", "level", "created_by", "created_at")
    list_filter = ("category", "level")
    inlines = [AnnouncementAttachmentInline]


@admin.register(AnnouncementSettings)
class AnnouncementSettingsAdmin(admin.ModelAdmin):
    list_display = ("auto_on_period",)
```

---

## 21-7. ✅ 動作確認

1. リーダーで「アナウンス管理」→ レベルを選び、画像とPDFを添付して投稿。
2. クルーでログイン → ホームに**未読バッジ**。一覧では消えず、**詳細を開くと既読**になる（クルーごと）。画像はインライン表示、PDFはリンク。
3. 添付URLは未ログインだと `/login/` へ。ログイン時のみ配信される。
4. 「確定シフト」ページの「アナウンスを投稿」で完了レベルのアナウンスが出る。
5. 期間追加（表示ON）で、設定ON時のみ自動投稿される。
6. 投稿日時を8日前にしたアナウンスは、一覧/ホームを開いた時点で消える。

### テスト

`shifts/tests.py` に、手動投稿・添付検証・未読/既読・自動投稿のオンオフ・確定ボタン投稿・自動削除・添付の認証配信を加えます。

```
$ python manage.py test shifts
```

---

## つまずきポイント

- **添付が保存されない**: フォームに `enctype="multipart/form-data"` が無い／`name="files"`（複数）とビューの `getlist("files")` が一致していない。
- **未読が減らない/増えない**: 既読は**詳細を開いたときだけ** `get_or_create`。一覧表示では付けません。`UniqueConstraint(announcement, user)` で二重登録を防いでいます。
- **添付が誰でも開ける**: 配信を `{{ att.file.url }}` 直リンクにしていないか確認。必ず `announcement_attachment` ビュー経由（7章と同じ）にします。

> これで講義は現行アプリの全機能をカバーします。全体像は [18章 全体のまとめ](18_全体のまとめ.md) を参照してください。
