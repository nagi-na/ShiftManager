# 8章 管理画面

最後に、リーダー用の管理画面を仕上げます。これでコア機能が完成します。

- **管理メニュー（S8）**: 管理機能への入口。
- **期間・締切設定（S7）**: 対象期間の作成・編集・締切・削除。
- **アカウント管理（S6）**: クルーの作成・編集・有効/無効・削除・パスワード再発行。
- **クルー自身の設定**: メール登録・パスワード変更。

> 🔰 /admin（Django標準管理画面）でもデータは触れますが、リーダーに毎回 /admin を使わせるのは危険（何でも消せる）かつ分かりにくい。だから**運用に必要な操作だけ**を絞った専用画面を自作します。

---

## 8-1. 管理メニュー（S8）

4章のスタブ `manage_top` を本実装にします。

ファイル: `shifts/views.py`

import に `User` を使うので（6章で追加済みなら不要）、`leader_required` と合わせて確認しておきます。本体:

```python
@leader_required
def manage_top(request):
    """S8 リーダー用トップ（管理メニュー）。"""
    now = timezone.now()
    staff_count = User.objects.filter(
        role=User.Role.STAFF, is_active=True
    ).count()
    open_count = sum(1 for p in ShiftPeriod.objects.all() if not _is_closed(p, now))
    return render(
        request,
        "shifts/manage_top.html",
        {"staff_count": staff_count, "open_count": open_count},
    )
```

ファイル: `shifts/templates/shifts/manage_top.html`（新規作成）

```html
{% extends "base.html" %}

{% block title %}管理メニュー | シフト提出{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-3">
  <h1 class="h4 mb-0">管理メニュー</h1>
  <a class="btn btn-outline-secondary btn-sm" href="{% url 'home' %}">← ホーム</a>
</div>

<div class="row g-3">
  <div class="col-12 col-md-6">
    <div class="card h-100 shadow-sm">
      <div class="card-body">
        <h2 class="h6">アカウント管理</h2>
        <p class="text-muted small mb-2">クルーの作成・編集・削除・パスワード再発行・有効/無効化</p>
        <p class="small mb-2">有効なクルー: {{ staff_count }} 名</p>
        <a class="btn btn-primary btn-sm" href="{% url 'manage_accounts' %}">アカウント管理へ</a>
      </div>
    </div>
  </div>

  <div class="col-12 col-md-6">
    <div class="card h-100 shadow-sm">
      <div class="card-body">
        <h2 class="h6">期間・締切設定</h2>
        <p class="text-muted small mb-2">対象期間の作成・編集・締切・削除</p>
        <p class="small mb-2">募集中の期間: {{ open_count }} 件</p>
        <a class="btn btn-primary btn-sm" href="{% url 'manage_periods' %}">期間・締切設定へ</a>
      </div>
    </div>
  </div>
</div>
{% endblock %}
```

---

## 8-2. 期間・締切設定（S7）

期間の作成（一覧と同じ画面のフォーム）・編集・締切切替・削除を作ります。

### フォーム

ファイル: `shifts/forms.py`（追記）

先頭importに `ShiftPeriod` を足します。

```python
from .models import TIME_CHOICES, ConfirmedShift, ShiftPeriod
```

末尾に:

```python
class PeriodForm(forms.ModelForm):
    """対象期間の作成・編集（S7）。"""

    class Meta:
        model = ShiftPeriod
        fields = ["title", "start_date", "end_date", "deadline", "status"]
        widgets = {
            "title": forms.TextInput(
                attrs={"class": "form-control", "placeholder": "例: 6/9〜6/15分"}
            ),
            "start_date": forms.DateInput(
                attrs={"type": "date", "class": "form-control"}, format="%Y-%m-%d"
            ),
            "end_date": forms.DateInput(
                attrs={"type": "date", "class": "form-control"}, format="%Y-%m-%d"
            ),
            "deadline": forms.DateTimeInput(
                attrs={"type": "datetime-local", "class": "form-control"},
                format="%Y-%m-%dT%H:%M",
            ),
            "status": forms.Select(attrs={"class": "form-select"}),
        }

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        # HTML の date / datetime-local 入力を受け取れるようにする
        self.fields["start_date"].input_formats = ["%Y-%m-%d"]
        self.fields["end_date"].input_formats = ["%Y-%m-%d"]
        self.fields["deadline"].input_formats = ["%Y-%m-%dT%H:%M"]
```

**解説**

- **HTML5の日付入力**: `type="date"` / `type="datetime-local"` でブラウザのカレンダーUIを使います。Django側は、その入力フォーマット（`%Y-%m-%d` 等）を `input_formats` と `widget...format` の両方で合わせる必要があります（表示と解釈の両方向のため）。
- 2章の `clean()`（終了日 < 開始日を弾く）は `ModelForm` が自動で呼びます。

### ビュー（作成・一覧・編集・締切・削除）

ファイル: `shifts/views.py`

import に `PeriodForm` を足します。

```python
from .forms import ConfirmedShiftForm, NoteForm, PeriodForm, ShiftDayFormSet
```

本体:

```python
@leader_required
def manage_periods(request):
    """S7 期間・締切設定（一覧＋新規作成）。"""
    if request.method == "POST":
        form = PeriodForm(request.POST)
        if form.is_valid():
            period = form.save(commit=False)
            period.created_by = request.user
            period.save()
            messages.success(request, "対象期間を作成しました。")
            return redirect("manage_periods")
    else:
        form = PeriodForm()

    now = timezone.now()
    items = [
        {
            "period": p,
            "is_closed": _is_closed(p, now),
            "request_count": p.requests.count(),
        }
        for p in ShiftPeriod.objects.all()
    ]
    return render(request, "shifts/manage_periods.html", {"items": items, "form": form})


@leader_required
def manage_period_edit(request, pk):
    """対象期間の編集。"""
    period = get_object_or_404(ShiftPeriod, pk=pk)
    if request.method == "POST":
        form = PeriodForm(request.POST, instance=period)
        if form.is_valid():
            form.save()
            messages.success(request, "対象期間を更新しました。")
            return redirect("manage_periods")
    else:
        form = PeriodForm(instance=period)
    return render(
        request, "shifts/manage_period_edit.html", {"form": form, "period": period}
    )


@leader_required
def manage_period_close(request, pk):
    """募集中⇔締切 を手動で切り替える。"""
    period = get_object_or_404(ShiftPeriod, pk=pk)
    if request.method == "POST":
        if period.status == ShiftPeriod.Status.CLOSED:
            period.status = ShiftPeriod.Status.OPEN
            msg = "募集中に戻しました。"
        else:
            period.status = ShiftPeriod.Status.CLOSED
            msg = "締め切りました。"
        period.save(update_fields=["status"])
        messages.success(request, msg)
    return redirect("manage_periods")


@leader_required
def manage_period_delete(request, pk):
    """対象期間の削除。提出・確定シフトも一緒に削除される。"""
    period = get_object_or_404(ShiftPeriod, pk=pk)
    if request.method == "POST":
        count = period.requests.count()
        for cs in period.confirmed_shifts.all():
            cs.file.delete(save=False)  # 実ファイルを先に削除
        title = str(period)
        period.delete()
        messages.success(request, f"「{title}」と提出 {count} 件を削除しました。")
    return redirect("manage_periods")
```

**解説**

- **作成と編集で同じ `PeriodForm`**: 新規は `PeriodForm(request.POST)`、編集は `instance=period` を渡すだけ。`created_by` は新規時にビューで埋めます。
- **`update_fields=["status"]`**: 締切切替では `status` だけ更新。`auto_now` の `updated_at` 以外を無駄に書き換えません。
- **削除は実ファイルも消す**: 期間を消すと、`on_delete=CASCADE` で提出・各日・確定シフトのDB行も連鎖削除されます。ただし**確定シフトの実ファイル**はDBと別なので、先に `cs.file.delete()` で消します。

### URL

ファイル: `shifts/urls.py`（`urlpatterns` に追記）

```python
    path("manage/periods/", views.manage_periods, name="manage_periods"),
    path("manage/periods/<int:pk>/edit/", views.manage_period_edit, name="manage_period_edit"),
    path("manage/periods/<int:pk>/close/", views.manage_period_close, name="manage_period_close"),
    path("manage/periods/<int:pk>/delete/", views.manage_period_delete, name="manage_period_delete"),
```

### テンプレート（一覧＋作成フォーム）

ファイル: `shifts/templates/shifts/manage_periods.html`（新規作成）

```html
{% extends "base.html" %}

{% block title %}期間・締切設定 | シフト提出{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-3">
  <h1 class="h4 mb-0">期間・締切設定</h1>
  <a class="btn btn-outline-secondary btn-sm" href="{% url 'manage_top' %}">← 管理メニュー</a>
</div>

<div class="card shadow-sm mb-4">
  <div class="card-body">
    <h2 class="h6 mb-3">新しい対象期間を作成</h2>
    <form method="post" class="row g-2">
      {% csrf_token %}
      <div class="col-12">
        <label class="form-label small mb-0">タイトル（任意）</label>
        {{ form.title }}
      </div>
      <div class="col-6 col-md-3">
        <label class="form-label small mb-0">開始日</label>
        {{ form.start_date }}
      </div>
      <div class="col-6 col-md-3">
        <label class="form-label small mb-0">終了日</label>
        {{ form.end_date }}
      </div>
      <div class="col-8 col-md-4">
        <label class="form-label small mb-0">締切日時</label>
        {{ form.deadline }}
      </div>
      <div class="col-4 col-md-2">
        <label class="form-label small mb-0">状態</label>
        {{ form.status }}
      </div>
      {% if form.errors %}
        <div class="col-12"><div class="alert alert-danger mb-0 py-2 small">
          {% for field, errs in form.errors.items %}{{ field }}: {{ errs|join:" / " }}<br>{% endfor %}
        </div></div>
      {% endif %}
      <div class="col-12">
        <button type="submit" class="btn btn-primary btn-sm">作成</button>
      </div>
    </form>
  </div>
</div>

<div class="card shadow-sm">
  <div class="table-responsive">
    <table class="table table-sm align-middle mb-0">
      <thead class="table-light">
        <tr>
          <th>タイトル</th>
          <th>期間</th>
          <th>締切</th>
          <th>状態</th>
          <th>提出</th>
          <th class="text-center">提出状況</th>
          <th>操作</th>
        </tr>
      </thead>
      <tbody>
        {% for item in items %}
        <tr>
          <td>{{ item.period.title|default:"(無題)" }}</td>
          <td class="text-nowrap small">{{ item.period.start_date }}〜{{ item.period.end_date }}</td>
          <td class="text-nowrap small">{{ item.period.deadline|date:"Y/m/d H:i" }}</td>
          <td>
            {% if item.is_closed %}<span class="badge bg-secondary">締切</span>
            {% else %}<span class="badge bg-success">募集中</span>{% endif %}
          </td>
          <td>{{ item.request_count }}</td>
          <td class="text-center">
            <a class="btn btn-outline-primary btn-sm" href="{% url 'period_status' item.period.pk %}">確認</a>
          </td>
          <td class="text-nowrap">
            <a class="btn btn-outline-secondary btn-sm" href="{% url 'manage_period_edit' item.period.pk %}">編集</a>
            <form method="post" action="{% url 'manage_period_close' item.period.pk %}" class="d-inline">
              {% csrf_token %}
              <button class="btn btn-outline-warning btn-sm" type="submit">
                {% if item.period.status == 'closed' %}募集再開{% else %}締切{% endif %}
              </button>
            </form>
            <form method="post" action="{% url 'manage_period_delete' item.period.pk %}" class="d-inline"
                  onsubmit="return confirm('この期間を削除します。{% if item.request_count %}提出 {{ item.request_count }} 件も一緒に削除されます。{% endif %}よろしいですか？');">
              {% csrf_token %}
              <button class="btn btn-outline-danger btn-sm" type="submit">削除</button>
            </form>
          </td>
        </tr>
        {% empty %}
        <tr><td colspan="7" class="text-muted">対象期間がありません。</td></tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
</div>
{% endblock %}
```

ファイル: `shifts/templates/shifts/manage_period_edit.html`（新規作成）

```html
{% extends "base.html" %}

{% block title %}期間の編集 | シフト提出{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-3">
  <h1 class="h4 mb-0">対象期間の編集</h1>
  <a class="btn btn-outline-secondary btn-sm" href="{% url 'manage_periods' %}">← 一覧へ</a>
</div>

<div class="card shadow-sm">
  <div class="card-body">
    <form method="post" class="row g-2">
      {% csrf_token %}
      <div class="col-12">
        <label class="form-label small mb-0">タイトル（任意）</label>
        {{ form.title }}
      </div>
      <div class="col-6">
        <label class="form-label small mb-0">開始日</label>
        {{ form.start_date }}
      </div>
      <div class="col-6">
        <label class="form-label small mb-0">終了日</label>
        {{ form.end_date }}
      </div>
      <div class="col-8">
        <label class="form-label small mb-0">締切日時</label>
        {{ form.deadline }}
      </div>
      <div class="col-4">
        <label class="form-label small mb-0">状態</label>
        {{ form.status }}
      </div>
      {% if form.errors %}
        <div class="col-12"><div class="alert alert-danger mb-0 py-2 small">
          {% for field, errs in form.errors.items %}{{ field }}: {{ errs|join:" / " }}<br>{% endfor %}
        </div></div>
      {% endif %}
      <div class="col-12 mt-2">
        <button type="submit" class="btn btn-primary btn-sm">保存</button>
      </div>
    </form>
  </div>
</div>
{% endblock %}
```

---

## 8-3. アカウント管理（S6）

クルーのアカウントをリーダーが発行・管理します。**初期パスワードは自動発行**し、画面に1回だけ表示して本人に伝える運用にします。

### フォーム

ファイル: `accounts/forms.py`（新規作成）

```python
from django import forms

from .models import User

_TEXT = {"class": "form-control"}
_SELECT = {"class": "form-select"}


class AccountCreateForm(forms.ModelForm):
    """アカウント新規作成。パスワードは作成時に自動発行する。"""

    class Meta:
        model = User
        fields = ["username", "name", "email", "role"]
        widgets = {
            "username": forms.TextInput(attrs=_TEXT),
            "name": forms.TextInput(attrs=_TEXT),
            "email": forms.EmailInput(attrs=_TEXT),
            "role": forms.Select(attrs=_SELECT),
        }
        labels = {"username": "ログインID"}


class AccountEditForm(forms.ModelForm):
    """アカウントの編集（ログインID・名前・メール・ロール・状態）。"""

    class Meta:
        model = User
        fields = ["username", "name", "email", "role", "is_active"]
        widgets = {
            "username": forms.TextInput(attrs=_TEXT),
            "name": forms.TextInput(attrs=_TEXT),
            "email": forms.EmailInput(attrs=_TEXT),
            "role": forms.Select(attrs=_SELECT),
            "is_active": forms.CheckboxInput(attrs={"class": "form-check-input"}),
        }
        labels = {"username": "ログインID", "is_active": "有効"}


class ProfileEmailForm(forms.ModelForm):
    """クルー自身によるメールアドレスの登録・変更（IDは変更不可）。"""

    class Meta:
        model = User
        fields = ["email"]
        widgets = {"email": forms.EmailInput(attrs=_TEXT)}
        labels = {"email": "メールアドレス"}
```

**解説**

- **作成フォームにパスワード欄が無い**: パスワードはユーザーに決めさせず、サーバーで自動生成します（後述）。
- `ProfileEmailForm` は `email` だけ。クルー本人が触れるのはメールのみ（IDやロールは変えさせない）。

### ビュー

ファイル: `accounts/views.py`（新規作成）

```python
from django.contrib import messages
from django.contrib.auth.decorators import login_required
from django.shortcuts import get_object_or_404, redirect, render
from django.utils.crypto import get_random_string

from .decorators import leader_required
from .forms import AccountCreateForm, AccountEditForm, ProfileEmailForm
from .models import User

# 紛らわしい文字（0/O/1/l/I など）を除いた、配布しやすい文字集合
_PW_CHARS = "abcdefghjkmnpqrstuvwxyzABCDEFGHJKLMNPQRSTUVWXYZ23456789"


def generate_password(length: int = 10) -> str:
    """配布用の初期パスワードを自動生成する。"""
    return get_random_string(length, _PW_CHARS)


@leader_required
def account_list(request):
    """S6 アカウント一覧＋新規作成（初期パスワードは自動発行）。"""
    if request.method == "POST":
        form = AccountCreateForm(request.POST)
        if form.is_valid():
            user = form.save(commit=False)
            password = generate_password()
            user.set_password(password)
            user.save()
            messages.success(
                request,
                f"アカウント「{user.name}」を作成しました。"
                f"初期パスワード: {password} （本人に伝えてください。この画面でのみ表示されます）",
            )
            return redirect("manage_accounts")
    else:
        form = AccountCreateForm(initial={"role": User.Role.STAFF})

    users = User.objects.order_by("role", "name", "username")
    return render(
        request, "accounts/account_list.html", {"users": users, "form": form}
    )


@leader_required
def account_edit(request, pk):
    """アカウントの編集。"""
    target = get_object_or_404(User, pk=pk)
    if request.method == "POST":
        form = AccountEditForm(request.POST, instance=target)
        if form.is_valid():
            # 自分自身の「有効」「リーダー権限」は外せない（ロックアウト防止）
            if target == request.user and (
                not form.cleaned_data["is_active"]
                or form.cleaned_data["role"] != User.Role.LEADER
            ):
                messages.error(
                    request, "自分自身の『有効』『リーダー権限』は変更できません。"
                )
            else:
                form.save()
                messages.success(request, "アカウントを更新しました。")
                return redirect("manage_accounts")
    else:
        form = AccountEditForm(instance=target)
    return render(request, "accounts/account_edit.html", {"target": target, "form": form})


@leader_required
def account_reset_password(request, pk):
    """パスワードを自動再発行する。"""
    target = get_object_or_404(User, pk=pk)
    if request.method == "POST":
        password = generate_password()
        target.set_password(password)
        target.save(update_fields=["password"])
        messages.success(
            request,
            f"「{target.name}」のパスワードを再発行しました。"
            f"新パスワード: {password} （本人に伝えてください）",
        )
    return redirect("manage_account_edit", pk=pk)


@leader_required
def account_toggle_active(request, pk):
    """有効/無効の切り替え。"""
    target = get_object_or_404(User, pk=pk)
    if request.method == "POST":
        if target == request.user:
            messages.error(request, "自分自身は無効化できません。")
        else:
            target.is_active = not target.is_active
            target.save(update_fields=["is_active"])
            state = "有効" if target.is_active else "無効"
            messages.success(request, f"「{target.name}」を{state}にしました。")
    return redirect("manage_accounts")


@leader_required
def account_delete(request, pk):
    """アカウントの削除。自分自身・データ作成者は削除不可。"""
    target = get_object_or_404(User, pk=pk)
    if request.method == "POST":
        if target == request.user:
            messages.error(request, "自分自身は削除できません。")
        elif target.created_periods.exists() or target.uploaded_shifts.exists():
            messages.error(
                request,
                "この利用者は期間の作成や確定シフトのアップロード履歴があるため削除できません。"
                "代わりに無効化してください。",
            )
        else:
            name = target.name or target.username
            target.delete()  # 提出データ（ShiftRequest等）は一緒に削除される
            messages.success(request, f"アカウント「{name}」を削除しました。")
    return redirect("manage_accounts")


@login_required
def profile(request):
    """クルー自身の設定（メール登録・変更）。ログインIDは変更不可。"""
    if request.method == "POST":
        form = ProfileEmailForm(request.POST, instance=request.user)
        if form.is_valid():
            form.save()
            messages.success(request, "メールアドレスを更新しました。")
            return redirect("profile")
    else:
        form = ProfileEmailForm(instance=request.user)
    return render(request, "accounts/profile.html", {"form": form})
```

**解説**

- **`set_password` を必ず使う**: パスワードは平文で保存してはいけません。`set_password` がハッシュ化して `password` フィールドに入れます。`user.password = ...` と直接代入してはダメ。
- **`get_random_string(length, chars)`**: Django同梱の安全な乱数生成。`_PW_CHARS` から紛らわしい文字（`0/O`, `1/l/I`）を除き、口頭・紙で配りやすくしています。
- **自分をロックアウトしない**: 編集で自分の「有効」を外す／リーダーを外すと、自分が管理画面に入れなくなります。`account_edit` でそれを防ぎます。`toggle_active` / `delete` でも「自分自身」をガード。
- **`PROTECT` との整合**: 期間作成者・アップロード者は `on_delete=PROTECT`（2章）なので、いきなり `delete()` するとDBエラーになります。先に `created_periods` / `uploaded_shifts` の有無を確認し、あるなら「無効化して」と案内します。

> 🔰 「削除」と「無効化」を使い分けます。退職者は基本**無効化**（データを残す）。完全削除は痕跡が無い人だけ、が安全です。

### クルー自身の設定・パスワード変更のURL

ファイル: `accounts/urls.py`（`urlpatterns` に追記。3章で作った login/logout はそのまま残す）

先頭importに `reverse_lazy` を足します。

```python
from django.contrib.auth import views as auth_views
from django.urls import path, reverse_lazy

from . import views
```

`urlpatterns` に追加:

```python
    # クルー自身の設定（メール変更・パスワード変更）
    path("profile/", views.profile, name="profile"),
    path(
        "profile/password/",
        auth_views.PasswordChangeView.as_view(
            template_name="accounts/password_change.html",
            success_url=reverse_lazy("password_change_done"),
        ),
        name="password_change",
    ),
    path(
        "profile/password/done/",
        auth_views.PasswordChangeDoneView.as_view(
            template_name="accounts/password_change_done.html"
        ),
        name="password_change_done",
    ),
    # アカウント管理（リーダー専用）
    path("manage/accounts/", views.account_list, name="manage_accounts"),
    path("manage/accounts/<int:pk>/edit/", views.account_edit, name="manage_account_edit"),
    path("manage/accounts/<int:pk>/reset-password/", views.account_reset_password, name="manage_account_reset"),
    path("manage/accounts/<int:pk>/toggle-active/", views.account_toggle_active, name="manage_account_toggle"),
    path("manage/accounts/<int:pk>/delete/", views.account_delete, name="manage_account_delete"),
```

**解説**

- **パスワード変更はDjango標準ビュー**: `PasswordChangeView` が「現在のパスワード確認＋新パスワードの強度チェック」をやってくれます。テンプレートと成功後の遷移先だけ指定します。
- `reverse_lazy` … URL解決をビュー定義時ではなく実行時まで遅らせる関数。URL設定の中でURL名を参照するときの定番です。

### ヘッダーに「設定」リンクを足す

クルーが自分の設定に行けるよう、`base.html` のナビバーに「設定」リンクを足します。3章で作った `base.html` のログアウトフォームの直前に1行追加します。

ファイル: `templates/base.html`

```html
        <a class="btn btn-sm btn-outline-light" href="{% url 'profile' %}">設定</a>
        <form method="post" action="{% url 'logout' %}" class="m-0">
```

（`<a ...>設定</a>` を、既存の `<form ...>ログアウト</form>` の直前に挿入）

---

## 8-4. アカウント管理のテンプレート

ファイル: `accounts/templates/accounts/account_list.html`（新規作成）

```html
{% extends "base.html" %}

{% block title %}アカウント管理 | シフト提出{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-3">
  <h1 class="h4 mb-0">アカウント管理</h1>
  <a class="btn btn-outline-secondary btn-sm" href="{% url 'manage_top' %}">← 管理メニュー</a>
</div>

<div class="card shadow-sm mb-4">
  <div class="card-body">
    <h2 class="h6 mb-3">新しいアカウントを作成</h2>
    <form method="post" class="row g-2">
      {% csrf_token %}
      <div class="col-6 col-md-3">
        <label class="form-label small mb-0">ログインID</label>
        {{ form.username }}
      </div>
      <div class="col-6 col-md-3">
        <label class="form-label small mb-0">名前</label>
        {{ form.name }}
      </div>
      <div class="col-6 col-md-3">
        <label class="form-label small mb-0">メール（任意）</label>
        {{ form.email }}
      </div>
      <div class="col-3 col-md-3">
        <label class="form-label small mb-0">ロール</label>
        {{ form.role }}
      </div>
      <div class="col-12">
        <div class="form-text">初期パスワードは作成時に自動発行され、作成後に画面に表示されます。</div>
      </div>
      {% if form.errors %}
        <div class="col-12"><div class="alert alert-danger mb-0 py-2 small">
          {% for field, errs in form.errors.items %}{{ field }}: {{ errs|join:" / " }}<br>{% endfor %}
        </div></div>
      {% endif %}
      <div class="col-12">
        <button type="submit" class="btn btn-primary btn-sm">作成</button>
      </div>
    </form>
  </div>
</div>

<div class="card shadow-sm">
  <div class="table-responsive">
    <table class="table table-sm align-middle mb-0">
      <thead class="table-light">
        <tr>
          <th>名前</th>
          <th>ログインID</th>
          <th>ロール</th>
          <th>メール</th>
          <th>状態</th>
          <th>操作</th>
        </tr>
      </thead>
      <tbody>
        {% for u in users %}
        <tr class="{% if not u.is_active %}table-secondary{% endif %}">
          <td>{{ u.name|default:"-" }}</td>
          <td>{{ u.username }}</td>
          <td>{{ u.get_role_display }}</td>
          <td class="small">{{ u.email|default:"-" }}</td>
          <td>
            {% if u.is_active %}<span class="badge bg-success">有効</span>
            {% else %}<span class="badge bg-secondary">無効</span>{% endif %}
          </td>
          <td class="text-nowrap">
            <a class="btn btn-outline-secondary btn-sm" href="{% url 'manage_account_edit' u.pk %}">編集</a>
            <form method="post" action="{% url 'manage_account_toggle' u.pk %}" class="d-inline">
              {% csrf_token %}
              <button class="btn btn-outline-warning btn-sm" type="submit"
                      {% if u == request.user %}disabled title="自分は変更不可"{% endif %}>
                {% if u.is_active %}無効化{% else %}有効化{% endif %}
              </button>
            </form>
            <form method="post" action="{% url 'manage_account_delete' u.pk %}" class="d-inline"
                  onsubmit="return confirm('「{{ u.name|default:u.username }}」を削除します。提出データも一緒に削除されます。よろしいですか？');">
              {% csrf_token %}
              <button class="btn btn-outline-danger btn-sm" type="submit"
                      {% if u == request.user %}disabled title="自分は削除不可"{% endif %}>削除</button>
            </form>
          </td>
        </tr>
        {% empty %}
        <tr><td colspan="6" class="text-muted">アカウントがありません。</td></tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
</div>
{% endblock %}
```

> 🔰 `{{ u.get_role_display }}` … `choices` 付きフィールドは `get_<field>_display` で**表示名**（「クルー」「リーダー」）を出せます。`u.role` だと中身の `"staff"` が出てしまいます。

ファイル: `accounts/templates/accounts/account_edit.html`（新規作成）

```html
{% extends "base.html" %}

{% block title %}アカウント編集 | シフト提出{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-3">
  <h1 class="h4 mb-0">アカウント編集</h1>
  <a class="btn btn-outline-secondary btn-sm" href="{% url 'manage_accounts' %}">← 一覧へ</a>
</div>

<div class="card shadow-sm mb-4">
  <div class="card-body">
    <h2 class="h6 mb-3">基本情報</h2>
    <form method="post" class="row g-2">
      {% csrf_token %}
      <div class="col-6">
        <label class="form-label small mb-0">ログインID</label>
        {{ form.username }}
      </div>
      <div class="col-6">
        <label class="form-label small mb-0">名前</label>
        {{ form.name }}
      </div>
      <div class="col-6">
        <label class="form-label small mb-0">メール（任意）</label>
        {{ form.email }}
      </div>
      <div class="col-3">
        <label class="form-label small mb-0">ロール</label>
        {{ form.role }}
      </div>
      <div class="col-3 d-flex align-items-end">
        <div class="form-check">
          {{ form.is_active }}
          <label class="form-check-label small">有効</label>
        </div>
      </div>
      {% if form.errors %}
        <div class="col-12"><div class="alert alert-danger mb-0 py-2 small">
          {% for field, errs in form.errors.items %}{{ field }}: {{ errs|join:" / " }}<br>{% endfor %}
        </div></div>
      {% endif %}
      <div class="col-12 mt-2">
        <button type="submit" class="btn btn-primary btn-sm">保存</button>
      </div>
    </form>
  </div>
</div>

<div class="card shadow-sm">
  <div class="card-body">
    <h2 class="h6 mb-2">パスワード再発行</h2>
    <p class="text-muted small mb-2">
      新しいパスワードを自動発行します。発行後に画面に表示されるので、本人に伝えてください。
    </p>
    <form method="post" action="{% url 'manage_account_reset' target.pk %}"
          onsubmit="return confirm('パスワードを再発行します。よろしいですか？');">
      {% csrf_token %}
      <button type="submit" class="btn btn-outline-danger btn-sm">パスワードを再発行</button>
    </form>
  </div>
</div>
{% endblock %}
```

---

## 8-5. クルー自身の設定・パスワード変更テンプレート

ファイル: `accounts/templates/accounts/profile.html`（新規作成）

```html
{% extends "base.html" %}

{% block title %}設定 | シフト提出{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-3">
  <h1 class="h4 mb-0">設定</h1>
  <a class="btn btn-outline-secondary btn-sm" href="{% url 'home' %}">← ホーム</a>
</div>

<div class="card shadow-sm mb-4">
  <div class="card-body">
    <h2 class="h6 mb-3">アカウント情報</h2>
    <div class="mb-2">
      <label class="form-label small mb-0">ログインID（変更できません）</label>
      <input type="text" class="form-control" value="{{ user.username }}" disabled>
    </div>
    <form method="post" class="row g-2">
      {% csrf_token %}
      <div class="col-12 col-md-8">
        <label class="form-label small mb-0">メールアドレス（任意）</label>
        {{ form.email }}
        {% if form.email.errors %}<div class="text-danger small">{{ form.email.errors|join:" / " }}</div>{% endif %}
        <div class="form-text">締切のお知らせなどに利用する予定です（任意）。</div>
      </div>
      <div class="col-12">
        <button type="submit" class="btn btn-primary btn-sm">メールを保存</button>
      </div>
    </form>
  </div>
</div>

<div class="card shadow-sm">
  <div class="card-body">
    <h2 class="h6 mb-2">パスワード</h2>
    <p class="text-muted small mb-2">初期パスワードから変更することをおすすめします。</p>
    <a class="btn btn-outline-primary btn-sm" href="{% url 'password_change' %}">パスワードを変更する</a>
  </div>
</div>
{% endblock %}
```

ファイル: `accounts/templates/accounts/password_change.html`（新規作成）

```html
{% extends "base.html" %}

{% block title %}パスワード変更 | シフト提出{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-3">
  <h1 class="h4 mb-0">パスワード変更</h1>
  <a class="btn btn-outline-secondary btn-sm" href="{% url 'profile' %}">← 設定へ</a>
</div>

<div class="row justify-content-center">
  <div class="col-12 col-md-6">
    <div class="card shadow-sm">
      <div class="card-body">
        {% if form.errors %}
          <div class="alert alert-danger small">入力内容を確認してください。</div>
        {% endif %}
        <form method="post">
          {% csrf_token %}
          <div class="mb-3">
            <label class="form-label" for="{{ form.old_password.id_for_label }}">現在のパスワード</label>
            <input type="password" name="{{ form.old_password.html_name }}"
                   id="{{ form.old_password.id_for_label }}" class="form-control" required>
            {% if form.old_password.errors %}<div class="text-danger small">{{ form.old_password.errors|join:" / " }}</div>{% endif %}
          </div>
          <div class="mb-3">
            <label class="form-label" for="{{ form.new_password1.id_for_label }}">新しいパスワード</label>
            <input type="password" name="{{ form.new_password1.html_name }}"
                   id="{{ form.new_password1.id_for_label }}" class="form-control" required>
            {% if form.new_password1.errors %}<div class="text-danger small">{{ form.new_password1.errors|join:" / " }}</div>{% endif %}
            {% if form.new_password1.help_text %}<div class="form-text">{{ form.new_password1.help_text|safe }}</div>{% endif %}
          </div>
          <div class="mb-3">
            <label class="form-label" for="{{ form.new_password2.id_for_label }}">新しいパスワード（確認）</label>
            <input type="password" name="{{ form.new_password2.html_name }}"
                   id="{{ form.new_password2.id_for_label }}" class="form-control" required>
            {% if form.new_password2.errors %}<div class="text-danger small">{{ form.new_password2.errors|join:" / " }}</div>{% endif %}
          </div>
          <button type="submit" class="btn btn-primary w-100">変更する</button>
        </form>
      </div>
    </div>
  </div>
</div>
{% endblock %}
```

ファイル: `accounts/templates/accounts/password_change_done.html`（新規作成）

```html
{% extends "base.html" %}

{% block title %}パスワード変更完了 | シフト提出{% endblock %}

{% block content %}
<div class="row justify-content-center">
  <div class="col-12 col-md-6">
    <div class="alert alert-success">パスワードを変更しました。</div>
    <a class="btn btn-primary btn-sm" href="{% url 'home' %}">ホームへ</a>
    <a class="btn btn-outline-secondary btn-sm" href="{% url 'profile' %}">設定へ</a>
  </div>
</div>
{% endblock %}
```

> 🔰 パスワードの**強度チェック**（短すぎる・よくある語など）は settings の `AUTH_PASSWORD_VALIDATORS` が担当します。`PasswordChangeView` がそれを使ってエラーを返してくれます。

---

## 8-6. 動作確認

### ✅ 管理機能ひととおり

1. リーダーでログイン → 「管理メニュー」。
2. **期間設定**: 新しい期間を作成 → 一覧に出る。「締切」を押すとバッジが「締切」に変わり、「募集再開」で戻る。「編集」で内容変更。「削除」は確認ダイアログ後に消える。終了日 < 開始日で作成しようとするとエラー。
3. **アカウント作成**: ログインID・名前を入れて作成 → 緑のメッセージに**初期パスワードが1回だけ表示**される。
4. その初期パスワードで、新しいクルーとして**別ブラウザ/シークレットウィンドウ**でログインできる。
5. クルーで「設定」→ メール登録、「パスワードを変更する」で変更 → 完了画面。
6. リーダーで、自分自身の「無効化」「削除」ボタンが**押せない**ことを確認。

---

## 8-7. つまずきポイント

- **作成したのにログインできない** → パスワードを `set_password` ではなく直接代入している。必ず `set_password`。
- **`get_role_display` が効かない** → `u.role` を書いている。表示名は `get_role_display`。
- **削除でエラー（IntegrityError / ProtectedError）** → そのユーザーが期間作成者など `PROTECT` 参照を持つ。ビューで事前チェックして無効化に誘導。
- **自分を無効化して入れなくなった** → /admin か `python manage.py shell` で `is_active=True` / `role='leader'` に戻す。本章のガードはこれを防ぐためのもの。

---

## この章のまとめ（コア完成！）

- 管理メニュー・期間設定・アカウント管理・本人設定をすべて自作画面で実装した。
- パスワードは自動発行＋`set_password`（ハッシュ化）で安全に扱った。
- 「削除 vs 無効化」「自分のロックアウト防止」「`PROTECT` 参照の事前チェック」という運用上の勘所を押さえた。

これで **0〜8章のコア＋管理画面が完成**です。ログイン・提出・集計・確定シフト・管理がひと通り動きます。

ここから先は開発編の後半。実運用に耐えるよう、権限を3段階に増やし、締切後ルールや表示/非表示を足し、さらにGit管理・自動テストへ進みます（その後、本番編でデプロイ）。

➡️ [9章 権限と提出ルール（発展機能）](09_権限と提出ルール.md)
